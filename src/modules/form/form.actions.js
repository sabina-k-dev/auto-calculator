import { createAction } from "redux-act";

export const changeForm = createAction("change form values");
export const changePage = createAction("change form page");
export const changeNextButtons = createAction("change 'is disabled' buttons");
